import 'package:flutter/material.dart';

class StoryDetailPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('قصة النبي | Story Detail')),
      body: Center(
        child: Text('Détail de l\'histoire du prophète'),
      ),
    );
  }
} 