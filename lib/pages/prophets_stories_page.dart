import 'package:flutter/material.dart';

class ProphetsStoriesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('قصص الأنبياء | Prophets Stories')),
      body: Center(
        child: Text('Liste des histoires des prophètes'),
      ),
    );
  }
} 