import 'package:flutter/material.dart';

class MiraclesPage extends StatelessWidget {
  const MiraclesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('إعجاز علمي في القرآن')),
      body: const Center(child: Text('صفحة الإعجاز العلمي')), 
    );
  }
} 